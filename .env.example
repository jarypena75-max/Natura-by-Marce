# SQLite for local dev. For production, consider Postgres (Supabase/Neon) and update Prisma datasource.
DATABASE_URL="file:./dev.db"

# WhatsApp phone (country code + number, no +, no spaces). Example Mexico: 5215512345678
WHATSAPP_PHONE="5215512345678"

# Admin password for /admin (change this!)
ADMIN_PASSWORD="cambia_esto_por_una_clave_fuerte"

# Secret for signing auth cookie (change this!)
AUTH_SECRET="pon_un_secreto_largo_aleatorio"
